# Dokimos self-observations — 2026-09-24

These are the first longitudinal observations produced while building Dokimos itself.

## Interpretation boundary

The files record measured CI evidence. They do not retroactively claim metrics that were not collected.

The series of CI failures during repository bootstrapping shared the same pre-existing F# compilation defect. They are not counted as independent introduced regressions. The representative failed snapshot preserves the defect evidence, while the first green snapshot records its resolution.

## First transition

- Failed state: 12 compiler errors; tests unavailable because build failed.
- Corrective change: explicit `Observation` parameter typing in the comparison engine.
- First green state: build succeeded with 0 warnings and 0 errors; 4 of 4 tests passed.
- Finding lifecycle: `finding.fs-record-inference` moved from present to resolved.

This directory is evidence, not a marketing scorecard. Future snapshots should be generated by Dokimos rather than hand-authored once the collector is executable.
